# 64tojson
A simple and intuitive web tool that converts Base64-encoded strings into readable JSON format. Includes a built-in JSON viewer to explore and validate the decoded data. Perfect for debugging, development, or inspecting encoded API payloads.
